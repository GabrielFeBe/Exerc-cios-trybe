const notas = 80;
if (notas >= 80) {
    console.log('Parabéns, você foi aprovada(o)!');
} else if (notas < 80 && notas >= 60) {
    console.log('Você está na nossa lista de espera');
} else {
    console.log('Você foi reprovada(o)');
}